apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - deployment.yaml
  - service-clusterip.yaml
  - service-nodeport.yaml
  # - service-loadbalancer.yaml  # Uncomment when running where LB provisioning is supported
